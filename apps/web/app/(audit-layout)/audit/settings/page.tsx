import { SettingsPanel } from '@/components/settings/SettingsPanel'

// Auditor settings — rendered inside the audit mobile shell (sticky header)
// so auditors can return to their work without the browser back button.
export default function AuditSettingsPage() {
  return <SettingsPanel />
}

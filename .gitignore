# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Python (checklist migration tool)
tools/checklist-migration/.venv
tools/checklist-migration/__pycache__
tools/checklist-migration/.pytest_cache
tools/checklist-migration/**/__pycache__

# checklist migration tool: real สนข. documents and generated checklist
# data (contains real checklist text) never get committed — code + tests
# with synthetic fixtures do.
tools/checklist-migration/input/
tools/checklist-migration/output/

# Historical checklist reimport tool (Part F, W2-S1): the real pre-cutover export (1,394
# checklists, real สนข. inspection data) is dropped here directly, never committed — same
# confidentiality rule as the checklist-migration tool above. Code + the synthetic fixture in
# apps/api/src/admin/reimport/__fixtures__/ are the only committed artifacts.
apps/api/docs/legacy-export/

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
